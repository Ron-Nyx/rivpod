name: some_app
description: some app.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  riverpod_annotation: ^2.3.0

dev_dependencies:
  build_runner: ^2.4.0
  custom_lint: ^0.6.0
  flutter_lints: ^3.0.0
  riverpod_generator: ^2.3.0
  riverpod_lint: ^2.3.0
